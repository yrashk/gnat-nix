# GNAT (GPL) derivation for Nix

## Requirements

If you intend to use GPS, make sure `fonts.enableFontDir` is set
to `true` in your NixOS configuration. Otherwise, chances are,
all text will be rendered as rectangulars.
